<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- https://easyappicon.com/ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:name="app.dure.sijang.DureSijangApplication"
        android:allowBackup="true"
        android:icon="@mipmap/dure_sijang"
        android:label="Dure-Sijang"
        android:roundIcon="@mipmap/dure_sijang"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <activity
            android:name="android.app.NativeActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:resizeableActivity="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data android:name="android.app.lib_name" android:value="dure_sijang" />
        </activity>

        <!-- WebView Activity for Activity Embedding split-screen -->
        <activity
            android:name="app.dure.sijang.WebViewActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:resizeableActivity="true"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- Enable Activity Embedding for split-screen support -->
        <property
            android:name="android.window.PROPERTY_ACTIVITY_EMBEDDING_SPLITS_ENABLED"
            android:value="true" />

        <!-- Activity Embedding split configuration -->
        <meta-data
            android:name="androidx.window.embedding.splits"
            android:resource="@xml/main_split_config" />
    </application>

</manifest>